apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubeguard-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kubeguard
  template:
    metadata:
      labels:
        app: kubeguard
    spec:
      containers:
      - name: kubeguard-container
        image: kubeguard-ai:v99
        imagePullPolicy: Never
        ports:
        - containerPort: 5000
